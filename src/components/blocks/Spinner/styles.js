import { StyleSheet } from 'aphrodite'

export default StyleSheet.create({
  spinner: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    marginTop: '30px',
  },
})
